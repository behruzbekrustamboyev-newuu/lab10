#include <iostream>
using namespace std;

int reverseNumber(int num, int rev = 0) {
    if (num == 0)
        return rev;
    int digit = num % 10;
    return reverseNumber(num / 10, rev * 10 + digit);
}

int main() {
    int num;
    cout<<"Enter number" ;
    cin >> num;
    cout << reverseNumber(num);
    return 0;
}
